  # Implementation Plans

Generated by the improve skill on 2026-06-15. Last reconciled 2026-06-30 (HEAD `7fbccd1`).

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 007  | **Fix correctness issues and establish verification baseline** | **P0** | **M** | **—** | DONE (verified — see note) |
| 001  | PWA install prompt + offline support | P1 | S | 007 | DONE (verified) |
| 008  | Restore lint baseline (WishlistPanel assertion) | P0 | S | — | DONE — branch `advisor/008-restore-lint-baseline` (awaiting merge) |
| 002  | Transaction search, filter, and sort | P1 | S | 007 | TODO (excerpts drifted) |
| 003  | Notification center for upcoming bills | P2 | S | 007, 008 | DONE — branch `improve/003-notification-center` (awaiting merge) |
| 004  | Receipt photo uploads | P2 | S | 007 | TODO |
| 005  | Budgets and spending limits | P2 | M | 007, 008 | DONE — branch `improve/005-budgets-clean` (awaiting merge + `db:migrate`) |
| 006  | Multi-currency exchange rate auto-fetch | P2 | M | 007 | DONE (implemented independently) |

## Executed this session — branches awaiting your merge (2026-06-30)

All three were built and verified by dispatched executors in isolated worktrees, reviewed and approved. **None are pushed or merged — merging is your call.** All are based on local `main` (`7fbccd1`).

| Branch | Plan | Commit | Verified | Notes |
|--------|------|--------|----------|-------|
| `advisor/008-restore-lint-baseline` | 008 | `378443b` | `tsc` 0, `lint` 0 errors | 1-line fix; merge FIRST to clean the lint baseline |
| `improve/003-notification-center` | 003 | `f39559e` | `tsc` 0, eslint 0 | new `BillsPopover.tsx` + header swap; only 2 files |
| `improve/005-budgets-clean` | 005 | `cbc2966` | `tsc` 0, eslint 0 | full-stack; run `bun run db:generate && bun run db:migrate` after merge (migration `drizzle/0007_absurd_cobalt_man.sql` already generated) |

**Merge order:** 008 → 003 → 005. **003 and 005 both edit `FinanceApp.tsx`** (003 the header bell→popover; 005 the nav/tabs + `useFinanceData` destructure), so whichever you merge second needs a trivial conflict resolution there.

**Stale clutter to ignore/delete** (left over from the worktree base mix-up — they sit on the divergent `origin/main` line `85f8c36`, NOT on your local main, so do not merge them): branches `advisor/003-notification-center` (`6530de4`), `advisor/005-budgets` (`85f8c36`), and the `worktree-agent-*` branches.

**Base caveat:** `isolation: worktree` created worktrees on the `origin/main` tip (`85f8c36`), which lacks your unpushed local currency work (`214db16`, `7fbccd1`). The approved branches above were re-homed onto `7fbccd1` and re-verified. Reconcile local `main` vs `origin/main` when convenient.

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale — finding fixed independently or approach abandoned)

## Reconcile log — 2026-06-30

- **007 — DONE, verified with one caveat.** Merged via PR #1. `tsc --noEmit` passes at HEAD. Five of six fixes hold (scheduler removed from routes, `syncLabels` in `updateTransaction`, unused `inArray` removed, CLI/seed/FinanceApp/OperationDialog type fixes). The `billingDay: max(28)` fix was intentionally superseded by `5bcc8a9` (calendar-aware `getNextBillingDate()`, validator back to `1-31`) — documented in the plan. **Regression found:** `bun run lint` no longer exits 0 — one error in `src/features/wishlist/WishlistPanel.tsx:302` (unnecessary non-null assertion) introduced by the post-007 currency work (`7fbccd1`). Tracked as **plan 008** to restore the baseline.
- **001 — DONE, verified.** Merged via PR #2. `vite-plugin-pwa` present, manifest link in `__root.tsx`, `InstallPrompt.tsx` exists and is rendered in the header. Implemented with the `injectManifest` strategy rather than the plan's `generateSW`/`autoUpdate` suggestion — a documented deviation; feature delivered.
- **006 — DONE, implemented independently (not via this plan).** Commits `214db16` + `7fbccd1` shipped multi-currency exchange rates and conversion using a different architecture than the plan: a dedicated `src/server/exchange-rates.ts` (Frankfurter fetch, `getExchangeRates`, `ensureFreshRates`, `convertAmount`), an `exchangeRates` tRPC query, and per-row conversion via `format.ts` helpers (`buildRateMap`/`convertAmountMinor`/`getRateFactor`) applied across all panels — rather than aggregation-level normalization behind a toggle. The feature is delivered; **do NOT execute plan 006** (its grep-based done criteria reference function names that were never used). Plan file retained as the historical record.
- **002, 004 — still TODO.** Neither implemented. 002's "Current state" excerpts have drifted (TransactionsPanel was heavily reworked by the currency conversion in `7fbccd1`); re-derive excerpts before executing. 004's "Attach receipt photo" dead label is unchanged and its excerpts are still accurate.
- **003, 005 — still TODO**, refreshed to HEAD and queued for execution.

## Recommended execution order

1. **008** — Restore lint baseline (P0 — unblocks clean `bun run lint` verification for everything else)
2. **003** — Notification center (turns the dead Bell button into a feature)
3. **005** — Budgets (schema migration + full-stack feature)
4. **002** — Transaction search (re-derive drifted excerpts first)
5. **004** — Receipt photos (schema + dead label already waiting)

## Dependency notes

- 002–005 require **007** (clean `tsc --noEmit` / `bun run lint` so they can be verified). 007 is DONE; **008** restores the lint half of that baseline.
- **003 and 005 both modify `src/features/finance/FinanceApp.tsx`.** Executed in isolated worktrees they don't conflict, but merging the second after the first will require a trivial conflict resolution in the header / nav region.
- 002, 004 are independent of each other and of 003/005.

## Findings considered and rejected

- **REST API loads entire dashboard per request** (perf, M effort) — deferred; needs a targeted refactor of the API route handler.
- **Multi-write operations lack DB transactions** (correctness, M effort) — deferred; needs Drizzle transactions which require a different DB query pattern.
- **TanStack Query unused** (tech-debt, L effort) — deferred; a significant client-side refactor.
- **`transactions.subscriptionId` missing FK** (correctness, S effort) — deferred because it requires a schema migration; the user can apply it manually with the existing Drizzle migration workflow.
