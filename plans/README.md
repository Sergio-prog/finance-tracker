# Implementation Plans

Generated by the improve skill on 2026-06-15.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 007  | **Fix correctness issues and establish verification baseline** | **P0** | **M** | **—** | **DONE** |
| 001  | PWA install prompt + offline support | P1 | S | 007 | TODO |
| 002  | Transaction search, filter, and sort | P1 | S | 007 | TODO |
| 003  | Notification center for upcoming bills | P2 | S | 007 | TODO |
| 004  | Receipt photo uploads | P2 | S | 007 | TODO |
| 005  | Budgets and spending limits | P2 | M | 007 | TODO |
| 006  | Multi-currency exchange rate auto-fetch | P2 | M | 007 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale — finding fixed independently or approach abandoned)

## Recommended execution order

**007 must be first** — it fixes the broken verification baseline (0 tests, 16 type errors, 19 lint errors) so every subsequent plan can actually be verified with `tsc --noEmit` and `bun run lint`.

After 007, all 6 direction plans are independent. Suggested order:

1. **007** — Fix baseline (P0 — prerequisite for everything)
2. **001** — PWA install prompt (quick mobile win)
3. **002** — Transaction search (daily UX improvement)
4. **003** — Notification center (turns dead button into a feature)
5. **004** — Receipt photos (schema already waiting)
6. **005** — Budgets (requires schema migration)
7. **006** — Exchange rates (makes multi-currency meaningful)

## Dependency notes

- 001–006 all require **007** because without a clean `tsc --noEmit` and `bun run lint`, none of them can be verified.
- Plans 001–006 are independent of each other.

## Findings considered and rejected

- **REST API loads entire dashboard per request** (perf, M effort) — deferred; needs a targeted refactor of the API route handler.
- **Multi-write operations lack DB transactions** (correctness, M effort) — deferred; needs Drizzle transactions which require a different DB query pattern.
- **TanStack Query unused** (tech-debt, L effort) — deferred; a significant client-side refactor.
- **`transactions.subscriptionId` missing FK** (correctness, S effort) — deferred because it requires a schema migration; the user can apply it manually with the existing Drizzle migration workflow.
